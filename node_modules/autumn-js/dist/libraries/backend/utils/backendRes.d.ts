import { R as Result, A as AutumnError } from '../response-BBvvMvoC.js';

declare const toBackendRes: ({ res }: {
    res: Result<any, AutumnError>;
}) => {
    body: any;
    statusCode: number;
};
declare const toBackendError: ({ path, message, code, statusCode, }: {
    path: string;
    message: string;
    code?: string;
    statusCode?: number;
}) => {
    statusCode: number;
    body: AutumnError;
};

export { toBackendError, toBackendRes };
