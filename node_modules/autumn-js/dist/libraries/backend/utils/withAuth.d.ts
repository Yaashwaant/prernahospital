import { A as Autumn, C as CustomerData } from '../balancesTypes-D4pxt89N.js';
import { AuthResult } from './AuthFunction.js';
import 'zod/v4';
import '../response-BBvvMvoC.js';

declare const withAuth: <T extends {}>({ fn, requireCustomer, suppressLogs, }: {
    fn: (args: {
        autumn: Autumn;
        body: any;
        customer_id: string;
        customer_data?: CustomerData;
        pathParams?: Record<string, string>;
        searchParams?: Record<string, string>;
    }) => Promise<any>;
    requireCustomer?: boolean;
    suppressLogs?: boolean;
}) => ({ autumn, body, path, getCustomer, pathParams, searchParams, }: {
    autumn: Autumn;
    body: any;
    path: string;
    getCustomer: () => AuthResult;
    pathParams?: Record<string, string>;
    searchParams?: Record<string, string>;
}) => Promise<{
    body: any;
    statusCode: number;
}>;

export { withAuth };
