import * as swr from 'swr';
import { SWRConfiguration } from 'swr';
import { A as AutumnError } from '../error-DHbclCVh.mjs';
import { EventsListParams } from '../client/types/clientAnalyticsTypes.mjs';
import 'zod/v4';

declare const useListEvents: (params: EventsListParams & {
    swrConfig?: SWRConfiguration;
}) => {
    list: {
        id: string;
        timestamp: number;
        feature_id: string;
        customer_id: string;
        value: number;
        properties: Record<string, never>;
    }[] | undefined;
    hasMore: boolean;
    hasPrevious: boolean;
    page: number;
    isLoading: boolean;
    error: AutumnError | undefined;
    refetch: swr.KeyedMutator<{
        list: {
            id: string;
            timestamp: number;
            feature_id: string;
            customer_id: string;
            value: number;
            properties: Record<string, never>;
        }[];
        has_more: boolean;
        offset: number;
        limit: number;
        total: number;
    }>;
    nextPage: () => void;
    prevPage: () => void;
    goToPage: (pageNum: number) => void;
    resetPagination: () => void;
};

export { useListEvents };
