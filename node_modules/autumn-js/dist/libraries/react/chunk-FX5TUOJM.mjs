"use client";

import {
  BaseAutumnProvider
} from "./chunk-H27CEY2H.mjs";
import {
  AutumnContext
} from "./chunk-OYO7LPVB.mjs";
import {
  ConvexAutumnClient
} from "./chunk-FWAQWXWM.mjs";
import {
  AutumnClient
} from "./chunk-A3N7EAFE.mjs";

// src/libraries/react/ReactAutumnProvider.tsx
import { jsx } from "react/jsx-runtime";
var getBackendUrl = (backendUrl) => {
  if (backendUrl) {
    return backendUrl;
  }
  if (backendUrl && !backendUrl.startsWith("http")) {
    console.warn(`backendUrl is not a valid URL: ${backendUrl}`);
  }
  return "";
};
var ReactAutumnProvider = ({
  children,
  getBearerToken,
  convex,
  backendUrl,
  customerData,
  includeCredentials,
  betterAuthUrl,
  headers,
  convexApi,
  pathPrefix,
  suppressLogs
}) => {
  let client = convexApi ? new ConvexAutumnClient({
    convex,
    convexApi,
    customerData,
    headers,
    getBearerToken,
    suppressLogs
  }) : new AutumnClient({
    backendUrl: getBackendUrl(backendUrl),
    getBearerToken,
    customerData,
    includeCredentials,
    betterAuthUrl,
    headers,
    pathPrefix,
    suppressLogs
  });
  return /* @__PURE__ */ jsx(BaseAutumnProvider, { client, AutumnContext, children });
};

export {
  ReactAutumnProvider
};
