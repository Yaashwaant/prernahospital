"use client";


// src/libraries/react/client/types/clientEntTypes.ts
import { z } from "zod/v4";
var CreateEntityParamsSchema = z.object({
  id: z.string(),
  name: z.string().optional(),
  featureId: z.string()
});
var GetEntityParamsSchema = z.object({
  expand: z.array(z.string()).optional()
});
var EntityDataParamsSchema = z.object({
  name: z.string().optional(),
  featureId: z.string()
});
export {
  CreateEntityParamsSchema,
  EntityDataParamsSchema,
  GetEntityParamsSchema
};
