import type { Feature, Product } from "../compose/index.js";
export default function Push({ config, yes, prod, }: {
    config: {
        features: Feature[];
        products: Product[];
        env: string;
    };
    yes: boolean;
    prod: boolean;
}): Promise<void>;
